---
name: adb-ready
description: "ADB Entry, REVIEW and COMPLETION workflow; follows the project's canonical method."
---

<!-- Method command: adb-ready -->
# /adb-ready

Read this project's `AGENTS.md` and `METHOD.md`. For `METHOD: ADB`, follow `ADB.md` Entry, REVIEW and COMPLETION. In the method factory, the canonical method is `Methods/ADB/SKILL.md`. This command defines no independent rules.
